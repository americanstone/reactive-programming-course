Сформируйте серию `Flux`/`Mono`:
- Верните пустой `Flux` 
- Верните `Flux` который никогда не завершиться 
- Верните пустой `Mono` 
- Верните `Mono` который никогда не завершиться 
   
<div class="hint">
  Используйте Flux./Mono. never и Flux./Mono. empty
</div>